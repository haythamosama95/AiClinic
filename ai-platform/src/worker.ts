/**
 * A1 skeleton — health endpoint and binding checks land in Phase 3 (T007/T008).
 */
export class GatewayObject { }

export default {
  async fetch(): Promise<Response> {
    return new Response("not implemented", { status: 501 });
  },
};

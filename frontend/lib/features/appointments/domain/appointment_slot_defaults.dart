/// Default calendar grid interval and supported alternatives for booking slots.
const defaultTimeIntervalMinutes = 30;

/// Selectable grid intervals exposed in the calendar header.
const supportedTimeIntervalMinutes = <int>[15, 30, 60];

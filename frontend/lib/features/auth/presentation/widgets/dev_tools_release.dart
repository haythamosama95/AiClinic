import 'package:flutter/widgets.dart';

/// Stub widgets that render nothing in release builds.
/// These replace real dev tool widgets via conditional import.
class DevFillDummyClinicButton extends SizedBox {
  const DevFillDummyClinicButton({super.key});
}

class DevResetClinicButton extends SizedBox {
  const DevResetClinicButton({super.key});
}

class DevQuickAdminSignInButton extends SizedBox {
  const DevQuickAdminSignInButton({super.key});
}

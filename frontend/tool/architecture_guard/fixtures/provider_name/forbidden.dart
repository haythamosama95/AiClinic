// Fixture for guard_provider_name_fails_build (T2).
// Lives outside clean scan roots — invoked only in expect-fail CI runs.
// Uses the platform's first integrated provider id (D5 slice).

/// Representative provider name that must fail the architecture guard.
const String forbiddenProviderName = 'deepseek';

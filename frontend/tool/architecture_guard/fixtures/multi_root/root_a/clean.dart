// Multi-root probe: this root stays clean so a violation in root_b alone
// still proves the guard scanned both configured roots (Missing Tests #4).

const String cleanMarker = 'no forbidden content here';

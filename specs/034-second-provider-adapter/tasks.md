# Tasks: Second provider adapter (D7)

**Input**: Design documents from `specs/034-second-provider-adapter/`

**Prerequisites**: `plan.md` (required), `spec.md` (required for the user story). `research.md` is never produced on this platform (the research is `docs/architecture/17-ai-platform.md`). `data-model.md` is not produced — D7 defines no D1 entities (spec Key Entities). `contracts/second-provider-adapter.md` is already frozen on disk (written during the plan phase per DP-4). `quickstart.md` is written in Phase 4 (Documentation).

**Tests**: Tests are mandatory on this platform (delivery plan §3.10). Every named test in the spec's active `### Test plan` (T1–T11, T13, T14) is covered by its own task, written to fail before the code exists. Spy cases T8, T10, and T11 are separate tasks. T4 expands to one named subcase per Gemini wire error class enumerated in `contracts/second-provider-adapter.md` §3.1 (`auth_rejected`, `rate_limited`, `provider_server_error`, `content_filtered`) — still one task, four subcases. **T12 is excluded** — `blocked_on_F1` (spec Deferred tests); no task, no FR-010 / SC-005 trace.

**Organization**: One user story (US1, P1) — D7 is one slice, one story (delivery plan §2.6, overrides). No cross-story parallelism section. No Setup phase — contracts are already on disk; no migrations or `wrangler.toml` edits required by the plan. No Foundational or Polish phase.

**Task count**: 19 (≤25).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: US1 (this slice has a single user story)
- Include exact file paths in descriptions

## Path Conventions

- **AI gateway Worker**: `ai-platform/src/`, `ai-platform/migrations/`, `ai-platform/test/`
- **Spec Kit artifacts**: `specs/034-second-provider-adapter/`
- The Worker lives in `ai-platform/` at the repository root, a sibling of `frontend/` and `backend/` (delivery plan §7.1). The template's `frontend/lib/` and `backend/migrations/` conventions do not apply to this slice; D7 touches neither. No `ai-platform/migrations/` edits — D7 adds no D1 schema. Routing-policy registration is data under `ai-platform/control/routing-policy/`.

---

## Phase 1: Tests (written to fail before the code exists)

**Purpose**: One task covering each named test in the spec's active `### Test plan` (§3.11.4 row D7 = "Adapter fixtures + evals" — eval case deferred with F1; §13.5 Provider adapter tests — recorded provider fixtures). T1–T11 live in `ai-platform/test/gemini-adapter.test.ts`; T13–T14 live in `ai-platform/test/second-provider-policy.test.ts`. Both use the default Node-pool config (`vitest.config.ts`, `include: ["test/**/*.test.ts"]`); no workers-pool registration — transport and secret store are injectable (Clarification Q6 / plan), and the permanent suite uses recorded fixtures with no live egress. T001 creates `gemini-adapter.test.ts` and its substrate; T002–T011 append to it. T012 creates `second-provider-policy.test.ts` and its substrate; T013 appends to it. Modules under test (`../src/provider/gemini`, `../src/provider/wiring`) and the policy document do not exist yet, so the files fail to compile / fail assertions from the first importing task onward — the intended red state. Order follows the plan's Sequencing (surface/secret → mapping → stream/usage → errors → credential spies → policy/wiring).

- [X] T001 [US1] Add `T-D7-09 second_adapter_owns_no_retry_or_fallback` to `ai-platform/test/gemini-adapter.test.ts` (adapter fixtures): create the substrate — imports of `GeminiAdapter` from `../src/provider/gemini` and `ProviderPort` from `../src/provider/port`; a constructor call site taking injectable transport/fetch and secret-store ports (Clarification Q6 — duplicate D5 inject pattern without modifying `deepseek.ts`); optional recording logger and journal sinks for later spy cases. Then the case: assert the Gemini adapter export surface exposes classification only — inspect exported keys / types for absence of retry and fallback decision APIs (§4.3.8; §3.10 inherited prohibition). Satisfies FR-003 / SC-004. Proves T9.
- [X] T002 [US1] Add `T-D7-11 second_provider_credentials_from_secret_store_only` to `ai-platform/test/gemini-adapter.test.ts` (adapter fixtures, spy): inject a fake secret-store binding that records reads and returns a known secret value; construct `GeminiAdapter` with that store (and a transport stub); invoke; assert the secret was read from the store and that credentials were not taken from config files, request input, or hard-coded literals (§4.3.8; Clarification Q6). Satisfies FR-008 / SC-003. Proves T11.
- [X] T003 [US1] Add `T-D7-01 second_provider_request_mapping_golden` to `ai-platform/test/gemini-adapter.test.ts` (adapter fixtures): load a canonical request fixture and the recorded outbound wire golden under `ai-platform/test/fixtures/gemini/request-mapping/`; inject a transport that captures the outbound request; invoke the adapter; assert the emitted wire request (URL/method/headers/body shape) matches the golden, with auth sourced from the secret-store value and the secret not placed in mapped body fields (§4.3.8; §3.11.4 D7 ← D5). Satisfies FR-001, FR-002, FR-005 / SC-001. Proves T1.
- [X] T004 [US1] Add `T-D7-02 second_provider_stream_normalization` to `ai-platform/test/gemini-adapter.test.ts` (adapter fixtures): feed a recorded provider stream pair from `ai-platform/test/fixtures/gemini/stream/` through the injectable transport; invoke; assert every emitted chunk is a `CanonicalStreamChunk` with no provider-shaped field names at the port/adapter boundary (`assertNoProviderShapedFieldNames` or equivalent) (§4.3.8; §3.11.4 D7 ← D5). Satisfies FR-001, FR-005 / SC-001. Proves T2.
- [X] T005 [US1] Add `T-D7-03 second_provider_usage_extraction` to `ai-platform/test/gemini-adapter.test.ts` (adapter fixtures): feed a recorded usage-bearing response from `ai-platform/test/fixtures/gemini/usage/` through the injectable transport; invoke; assert usage counters land in the canonical usage form on the result / usage chunk (§4.3.8; §3.11.4 D7 ← D5). Satisfies FR-001, FR-005 / SC-001. Proves T3.
- [X] T006 [US1] Add `T-D7-04 second_provider_error_class_mapped_to_taxonomy` to `ai-platform/test/gemini-adapter.test.ts` (adapter fixtures): one named subcase per Gemini wire error class in `contracts/second-provider-adapter.md` §3.1 — `auth_rejected` → `provider_rejected` (terminal), `rate_limited` → `rate_limited` (retryable), `provider_server_error` → `internal_error` (retryable), `content_filtered` → `provider_rejected` (terminal) — each driven by a recorded failure under `ai-platform/test/fixtures/gemini/errors/`; assert taxonomy code and D2 retryability via `classifyFailure` / `setRetryabilityFromClassification`; no new taxonomy codes (§4.3.8; §3.11.4 D7 ← D5; §3.10). Satisfies FR-006 / SC-002. Proves T4.
- [X] T007 [US1] Add `T-D7-05 second_provider_malformed_response` to `ai-platform/test/gemini-adapter.test.ts` (adapter fixtures): feed a recorded malformed body from `ai-platform/test/fixtures/gemini/malformed/`; assert the outcome is a classified/normalized canonical error (or the port's required failure form), not an unclassified throw (§4.3.8; §3.11.4 D7 ← D5). Satisfies FR-005 / SC-002. Proves T5.
- [X] T008 [US1] Add `T-D7-06 second_provider_truncated_response` to `ai-platform/test/gemini-adapter.test.ts` (adapter fixtures): feed a recorded truncated body/finish from `ai-platform/test/fixtures/gemini/truncated/`; assert the outcome is port-normalized (classified failure or truncated result as the port contract requires) without inventing a new taxonomy code (§4.3.8; §3.11.4 D7 ← D5). Satisfies FR-005 / SC-002. Proves T6.
- [X] T009 [US1] Add `T-D7-07 second_provider_timeout` to `ai-platform/test/gemini-adapter.test.ts` (adapter fixtures): drive the adapter-owned timeout path with a recorded/simulated deadline-exceeded harness input from `ai-platform/test/fixtures/gemini/timeout/` (or transport that never resolves within the deadline); assert classification into taxonomy `timeout` with D2 retryability (§4.3.8; §3.11.4 D7 ← D5). Satisfies FR-007 / SC-002. Proves T7.
- [X] T010 [US1] Add `T-D7-08 second_provider_credentials_absent_from_logs_and_journal` to `ai-platform/test/gemini-adapter.test.ts` (adapter fixtures, spy): inject fake secret store + recording logger + recording journal sinks; invoke across success and failure paths; assert the known secret value appears in no collected log line and no journalable record (§4.3.8; §3.11.4 D7 ← D5; Done when). Satisfies FR-008, FR-009 / SC-003. Proves T8.
- [X] T011 [US1] Add `T-D7-10 second_adapter_owns_no_logging_policy` to `ai-platform/test/gemini-adapter.test.ts` (adapter fixtures, spy): assert the adapter does not own logging policy (no logging-policy API / decision surface on the export; emissions go only through injected sinks when present) and that credential/diagnostic emissions still satisfy T8's absence assertions (§4.3.8; §3.10). Satisfies FR-003 / SC-004. Proves T10.
- [X] T012 [P] [US1] Add `T-D7-13 added_by_routing_policy_edit_no_pipeline_diff` to `ai-platform/test/second-provider-policy.test.ts` (adapter fixtures + policy / structural): create the substrate — imports needed to assert structural path allowlist and to load wiring / policy artifacts. Then the case: assert only second-adapter + routing-policy data (+ provider wiring map) may be required to add Gemini; fail if invocation / retry-fallback / stream-broker / validator / journal pipeline modules must change (Clarification Q3; §4.3.8 adapter owns no fallback; delivery plan §3.5 Done when; §3.11.4 D7). Satisfies FR-011 / SC-006. Proves T13. `[P]` vs T001–T011 — different test file.
- [X] T013 [US1] Add `T-D7-14 fallback_ordering_honoured` to `ai-platform/test/second-provider-policy.test.ts` (unit — router + policy): drive unchanged `selectCandidateChain` from `../src/router` with the versioned policy document at `ai-platform/control/routing-policy/platform-default/1.json` (listing Gemini as low-priority fallback after higher-priority targets); assert the ordered chain places `gemini` after those targets, selection reason is recorded, and identical inputs yield an identical chain (Consumes D2 routing-policy-as-data; §3.11.4 D7). Satisfies FR-012 / SC-006. Proves T14.

---

## Phase 2: Implementation (plan Files section)

**Purpose**: The four implementation units named in `plan.md` → Files that do not yet exist: `gemini.ts` (Gemini adapter behind `ProviderPort`), `test/fixtures/gemini/**` (recorded request/response and stream pairs), `wiring.ts` (thin `provider_id` → adapter map), and `control/routing-policy/platform-default/1.json` (versioned policy listing Gemini as low-priority fallback). Consumed modules (`provider/port.ts`, `classify.ts`, `fake.ts`, `deepseek.ts`, `router/index.ts`, `contracts/canonical.ts`, `errors.ts`) are imported, not modified (delivery plan §2.3). Contracts are already frozen. Tests turn green in matching groups per the plan's Sequencing: T9/T11 with adapter skeleton + injectable ports; T1 with request mapping; T2/T3 with stream/usage; T4–T7 with error/malformed/truncated/timeout paths; T8/T10 with recording sinks; T13/T14 with policy data + wiring map.

- [X] T014 [US1] Create `ai-platform/src/provider/gemini.ts` — `GeminiAdapter` implements D2 `ProviderPort`. Constructor takes injectable transport/fetch and secret-store ports (Clarification Q6 — duplicate D5 inject pattern; do not modify `deepseek.ts`); optional recording logger/journal sinks for fixture spies only — adapter owns no logging policy. Owns authentication (API key from secret store binding name `GEMINI_API_KEY` in production wiring), canonical ↔ Gemini wire request/response mapping, stream-chunk normalization to `CanonicalStreamChunk`, provider-specific structured-output *wire* mechanics (pipeline `structured` / `structured_atomic` remain D6), adapter-owned timeouts, and classification of every failure via D2 `classifyFailure` / `setRetryabilityFromClassification` (no new taxonomy codes). Export surface exposes no retry or fallback decision API. Exactly one real provider in this module (`provider_id: gemini`); do not fold into DeepSeek or add a third provider (FR-004). No per-request server-side state; no Quota DO / D1 / R2 I/O. **Satisfies**: FR-001–FR-009; **proved by**: T1–T11 (T-D7-01..11).
- [X] T015 [P] [US1] Create recorded fixtures under `ai-platform/test/fixtures/gemini/` — directories and files for `request-mapping/` (canonical request + outbound wire golden for T1), `stream/` (provider stream pairs for T2), `usage/` (usage-bearing responses for T3), `errors/` (one recorded failure per §3.1 wire error class for T4), `malformed/` (T5), `truncated/` (T6), and `timeout/` (T7 harness input). Fixtures are the permanent proof — no live provider egress (FR-005, FR-006, FR-007; delivery plan §3.5 Done when adapter/fixture half). **Satisfies**: FR-005, FR-006, FR-007; **proved by**: T1–T7 (T-D7-01..07). Independent of T014's source file (different paths) once test stubs exist.
- [X] T016 [P] [US1] Create `ai-platform/src/provider/wiring.ts` — thin `provider_id` → adapter construction map covering DeepSeek and Gemini; allowlisted with adapter + policy data under T13; must not alter invocation / retry-fallback / stream-broker / validator / journal pipeline modules (Clarification Q3; FR-011). **Satisfies**: FR-011; **proved by**: T13 (T-D7-13). `[P]` vs T014/T015 — different file.
- [X] T017 [P] [US1] Create `ai-platform/control/routing-policy/platform-default/1.json` — versioned D2 routing-policy document listing Gemini as a low-priority fallback after higher-priority targets (unchanged schema meaning; router module not modified). **Satisfies**: FR-011, FR-012; **proved by**: T13, T14 (T-D7-13..14). `[P]` vs T014–T016 — different path (policy data).

---

## Phase 3: Verification

**Purpose**: Delivery plan §3.10 — every prior suite stays green alongside the new one, not just the latest.

- [X] T018 [US1] From `ai-platform/`, run `npx vitest run test/gemini-adapter.test.ts test/second-provider-policy.test.ts` (this slice's named cases, all under the default Node pool — D7 is CPU-only with injectable transport and secret store), then run the full prior suite — `npx vitest run` (default Node-pool prior suites including `gemini-adapter.test.ts`, `second-provider-policy.test.ts` (D7), `response-validator.test.ts`, `structured-modes.test.ts` (D6), `deepseek-adapter.test.ts` (D5), `stream-broker.test.ts` (D4), `invocation.test.ts` (D3), `provider-port.test.ts`, `router.test.ts` (D2), `prompt-registry.test.ts`, `prompt-composer.test.ts` (D1), `manifest.test.ts` (A4), `canonical.test.ts` (A3), `taxonomy.test.ts`, `reference.test.ts`, `error-body.test.ts`, `trace.test.ts`, `log-redaction.test.ts` (A2), `health.test.ts`, `env-deploys.test.ts` (A1), `context-validator.test.ts` (C2)) and `npx vitest run --config vitest.workers.config.ts` (workers-pool prior suites: `control.test.ts` (B2), `identity.test.ts`, `entitlement.test.ts`, `rate-limit.test.ts` (B3), `admission-credit.test.ts`, `quota-do.test.ts` (B4), `capability.test.ts` (C1), `journal.test.ts` (C3); `context.test.ts`, `config-cache.test.ts`, `migrations.test.ts` (A5); `adapter.test.ts` (A6)). Confirm every prior suite stays green. No new test is added here — this is the §3.10 checkpoint gate. **Satisfies**: the §3.10 checkpoint rule (wire mapping, stream normalization, usage extraction, one case per mapped error class, malformed, truncated, timeout, credential absence spies, no retry/fallback/logging-policy ownership, policy-data registration with no pipeline diff, fallback ordering). Proved by itself. Does **not** run or invent T12 / FR-010 / SC-005.

---

## Phase 4: Documentation

**Purpose**: Always present. The one documentation artifact the plan names that does not yet exist on disk (`quickstart.md`); written only after the suite is green. Contracts were already frozen during the plan phase. No other documentation artifact is named for a separate `[P]` task.

- [X] T019 [US1] Create `specs/034-second-provider-adapter/quickstart.md` from `.specify/templates/ai-platform-quickstart-template.md`, scoped to this slice only. **§1 Architecture context** — delivery plan §3.5 row D7; `17-ai-platform.md` §4.3.8 / §13.5; what the spec delivered; what the plan scoped. **§2 What was implemented** — Gemini real adapter behind the D2 port (`src/provider/gemini.ts`); recorded-fixture suite under `test/fixtures/gemini/`; secret-store credential path with absence spy; thin wiring map (`src/provider/wiring.ts`); routing-policy low-priority registration with no pipeline diff; fallback ordering; frozen `contracts/second-provider-adapter.md`; note that capability-eval acceptance (FR-010 / T12 / SC-005) is deferred to F1. **§3 Files to review** — only this slice's source, fixture, policy-data, test, and contract files (`ai-platform/src/provider/gemini.ts`, `ai-platform/src/provider/wiring.ts`, `ai-platform/control/routing-policy/platform-default/1.json`, `ai-platform/test/gemini-adapter.test.ts`, `ai-platform/test/second-provider-policy.test.ts`, `ai-platform/test/fixtures/gemini/`, `specs/034-second-provider-adapter/contracts/`). **§4 Prerequisites** — omit or keep minimal (`cd ai-platform && npm install` first time); CPU-only tests, no miniflare / live-provider bindings required for this slice. **§5 Run the automated suite** — `cd ai-platform && npx vitest run test/gemini-adapter.test.ts test/second-provider-policy.test.ts`; slice-only (no full-suite `npm test`). **§6 Inspect the changes** — read the frozen contract; open `provider/gemini.ts` and policy data; run the focused test files. **No §7** — CI is the only verification path (permanent suite uses recorded fixtures, not live egress). **Slice-only scope explicit**: no prior-slice files in the review table, no combined test counts, no prior-slice regression commands. Not traced to an FR (template-mandated review surface).

---

## Dependencies & Execution Order

### Phase Dependencies

- **Tests (T001–T013)** — none beyond already-frozen contracts and Consumes Binding modules; written to fail before the code exists. T001–T011 append to the same file (`gemini-adapter.test.ts`), so they are sequential (T001 creates the substrate; T002–T011 append). T012–T013 append to `second-provider-policy.test.ts` (T012 creates that substrate); T012 is `[P]` relative to the gemini-adapter file.
- **Implementation (T014–T017)** — after tests exist. T014 (`gemini.ts`), T015 (fixtures), T016 (`wiring.ts`), and T017 (policy JSON) are different paths and are `[P]`-eligible relative to each other once T001+ exist. Tests turn green in matching groups per the plan's Sequencing.
- **Verification (T018)** — depends on T001–T017; runs the whole suite (this slice + every prior slice) per §3.10.
- **Documentation (T019)** — depends on T018 (the quickstart records a green suite).

### Within the Slice

- Tests written to fail before implementation; implementation makes them pass; verification confirms the whole suite including prior slices stays green.
- Adapter skeleton + injectable ports (T014) unlock T9/T11 first; request mapping unlocks T1; stream/usage unlock T2/T3; error paths unlock T4–T7; recording sinks unlock T8/T10 — matching plan Sequencing without splitting the Files-section units into extra tasks.
- Fixtures (T015) unlock golden/stream/error file loads for T1–T7.
- Wiring map (T016) + policy data (T017) unlock T13/T14.

### Parallel Opportunities

- Phase 1: T001–T011 all touch `gemini-adapter.test.ts` — sequential; no `[P]` inside that file. T012 (`second-provider-policy.test.ts` substrate + T13) is `[P]` relative to T001–T011 — different file. T013 appends to the same policy test file after T012.
- Phase 2: T015 (`test/fixtures/gemini/**`), T016 (`src/provider/wiring.ts`), and T017 (`control/routing-policy/platform-default/1.json`) are `[P]` relative to T014 (`src/provider/gemini.ts`) and to each other — different paths, no import dependency between fixture JSON/text, wiring map, and policy data vs the adapter module source.
- Phase 4 (T019) is a single task — no `[P]`.

---

## Notes

- [P] tasks = different files, no dependencies. Marked: T012 (policy test file vs gemini-adapter tests), T015 (fixtures vs adapter source), T016 (wiring vs adapter/fixtures), T017 (policy data vs adapter/wiring/fixtures).
- Every named test T1–T11, T13, T14 from `spec.md` is covered by its own task; spy cases are not folded into outcome cases. **T12 / FR-010 / SC-005 are `blocked_on_F1` — no task traces to them.**
- Every task traces to an `FR-###` from `spec.md` (tests also state the `SC-###` they satisfy) — no task adds a requirement the spec does not name. Clarifications Q1–Q3, Q5–Q6 guide how (Gemini target, `src/provider/` path, T13 allowlist, inject pattern), not what.
- No Polish phase and no Foundational phase — prerequisites are the already-merged slices in the plan's Consumes Binding (D5; D2/A3 via D5).
- Consumed modules are imported, not modified (delivery plan §2.3 — extend, never rewrite). D5's DeepSeek adapter and suite remain unchanged. Pipeline modules (invocation, retry/fallback, stream broker, validator, journal) must not change to accommodate Gemini.
- Tests land before or alongside their implementation, never after (delivery plan §2.2).
- Preserve the I/O budget: no DO, D1, or R2 I/O from this slice; permanent suite uses recorded fixtures with injectable transport (§4.3.8, §4.4, §7.5, §13.6).
